{
  "name": "ar-scanner",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "build:guest": "npm run build --workspace=apps/guest-portal",
    "build:admin": "npm run build --workspace=apps/admin-suite"
  }
}
